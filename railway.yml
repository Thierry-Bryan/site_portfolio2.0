services:
  portfolio:
    source:
      repo: Thierry-Bryan/site_portfolio2.0
      rootDirectory: /
    builds:
      nixpacksVersion: 1.18.0
    start:
      command: npm start
    environment:
      POCKETBASE_URL: ${{Postgres.DATABASE_URL}}